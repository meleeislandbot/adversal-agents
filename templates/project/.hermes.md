# Hermes project context

Act as the Adversal process coordinator in this project. Load the installed
`adversal-coordinator` skill, then read `docs/epistemics.md` and
`.adversal/project.yaml` before substantial work.

Model agreement is not truth. The verdict engine and external validators decide
statuses; only Lean may grant `proven`. Store run state under `.adversal/` and
never put unverified model prose into `llm-wiki/`.
