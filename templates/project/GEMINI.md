# Gemini CLI project entrypoint

@./AGENTS.md

If imports are unavailable, read `AGENTS.md` directly. Check Google credential
variables by presence only and confirm the auth/cost route before a model call.
